DROP TABLE IF EXISTS HARDWARE;
CREATE TABLE HARDWARE (
	ID INT NOT NULL AUTO_INCREMENT,
	IP_HARDWARE VARCHAR(15) NOT NULL,
	STATUS BOOLEAN NOT NULL,
	LIGHT INT NOT NULL, /*tem que ser retirado, porque ligth virou uma table de fato.*/
	CAMERA INT NOT NULL,
	DIGITAL BOOLEAN,
	PRIMARY KEY (ID)
);
INSERT INTO hardware( ID,IP_HARDWARE,STATUS,LIGHT,CAMERA,DIGITAL) VALUES (1,'192.168.0.116', true, 16, 0, false);